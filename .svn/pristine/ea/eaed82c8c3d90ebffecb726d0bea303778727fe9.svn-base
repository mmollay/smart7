<?php

// Überprüfen, ob die aktuell ausgeführte Datei nicht login2.php ist
if (basename($_SERVER['PHP_SELF']) !== "login2.php") {
    include (__DIR__ . "/check_permission.php");
}

$host = 'localhost';
$dbname = 'ssi_trader';
$username = 'smart';
$password = 'Eiddswwenph21;';

// Verbindung zur Datenbank herstellen
$db = $GLOBALS['mysqli'] = mysqli_connect($host, $username, $password, $dbname);

// Fehlerbehandlung
if (!$db) {
    die ("Verbindung fehlgeschlagen: " . mysqli_connect_error());
}
